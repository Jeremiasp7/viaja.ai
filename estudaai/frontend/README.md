This folder is intended to hold the frontend for the `estudaai` instance.

Copy the top-level `frontend/` React app into this folder to give `estudaai` its own frontend.

Run it with:

```
cd estudaai/frontend
npm install
npm start
```
